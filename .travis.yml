language: java
jdk:
  - oraclejdk7
env:
  global:
    - TIMEOUT=6000000
before_script:
  - sudo apt-get install subversion
  - svn -h
  - cd fsplatform
script: 
  - mvn test
  - mvn package
after_script:
  - svn checkout https://svn.sinaapp.com/fsplatform/1 target --username hopesfish@163.com --password !@#cyrus!@# --no-auth-cache
  - cd target
  - mv -rf fsplatform-0.0.1-SNAPSHOT.war fsplatform.war
  - svn add fsplatform.war
  - ls -lah
  - svn commit -m "upload war" --username hopesfish@163.com --password !@#cyrus!@# --no-auth-cache
  - ping svn.sinaapp.com -i 10000
